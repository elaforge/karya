#!/bin/zsh
# This relies on ./bin/ being added to the PATH in zshrc but not zshenv to
# avoid recursion.  That's brittle, I should probably use .ghci magic to
# read the flags.

# Turn off idle time GC, since it causes input lag when lots of modules are
# loaded.
exec ghci -v0 +RTS -I0 -RTS $(cat build/debug/ghci-flags) \
    -fdefer-type-errors "$@"
