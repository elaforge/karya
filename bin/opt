#!/bin/sh
# Run an opt seq.  First make sure it's built and logview is running.

if repl=t bin/mk build/opt/logview build/opt/seq; then
    build/opt/logview &
    build/opt/seq "$@"
    if [[ $? -ne 0 ]]; then
        rm seq-repl
    fi
fi
