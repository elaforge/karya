#!/bin/sh
# Run a debug seq.  First make sure it's built and logview is running.

if repl=t bin/mk build/opt/logview build/debug/seq; then
    build/opt/logview &
    build/debug/seq "$@"
    if [[ $? -ne 0 ]]; then
        rm seq-repl
    fi
fi
